language: csharp
mono: none
dotnet: 6.0.300

branches:
  except:
  - /^experimental.*$/

matrix:
  include:
    - os: linux
      env: CONFIGURATION=Release
    - os: osx
      env: CONFIGURATION=Release

before_script:
  - dotnet --info

script:
  - dotnet test test/Aqua.Tests -c $CONFIGURATION -f net6.0